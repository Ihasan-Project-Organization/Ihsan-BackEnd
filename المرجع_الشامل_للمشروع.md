# المرجع الشامل النهائي — منصة إحسان لرعاية كبار السن

> **هذا الملف هو مصدر الحقيقة الوحيد (Single Source of Truth) لكل قرار تصميمي ووظيفي في المشروع حتى تاريخه.**
> يجب على أي ذكاء اصطناعي يعمل على هذا المشروع قراءة هذا الملف بالكامل قبل تنفيذ أي مهمة، والالتزام الحرفي بما فيه دون افتراض أي شيء من عنده.
> عند وجود أي تعارض بين هذا الملف وأي ملف/برومبت سابق، **هذا الملف هو المرجّح دائمًا** لأنه الأحدث.

---

## 1. نظرة عامة على المشروع

منصة ويب (Laravel + Tailwind CSS + JS عادي، عربي فقط RTL) تربط كبار السن بمقدمي خدمة (شباب) ضمن نظام هجين: الخدمة إما تطوعية أو مقابل أجر نقدي مباشر. الربط يتم ضمن نفس المدينة تقريبًا (بدون فلترة جغرافية آلية حاليًا). الهدف الأساسي: حل مشكلة الاعتماد العشوائي غير الموثوق على المعارف، ببديل أرخص من التوظيف الدائم وأكثر تنظيمًا.

## 2. الفئات الثلاث

1. **كبير السن** — طالب الخدمة. حساب واحد فقط، بلا حسابات فرعية للأبناء.
2. **مقدم الخدمة** — 18 سنة فأكثر إلزاميًا، يخضع لمراجعة إدارية.
3. **الإدارة** — بمستويين: Admin (تشغيلي) وSuper Admin (صلاحيات كاملة + إدارة حسابات الأدمن + إعدادات النظام).

## 3. الحسابات والاعتماد

- **كبير السن**: يسجّل عبر البريد الإلكتروني + كلمة المرور + الاسم + المدينة + رفع مستند إثبات هوية (صورة بطاقة شخصية). **يخضع لاعتماد إداري إلزامي** (حالة الحساب: `pending` → `approved`/`rejected`).
- **مقدم الخدمة**: يسجّل عبر البريد الإلكتروني + كلمة المرور + تاريخ الميلاد (تحقق تلقائي من 18+) + صورة هوية + شهادة حسن سيرة وسلوك. يخضع لنفس دورة الاعتماد الإداري.
- **لا يوجد حقل مدينة/سكن لمقدم الخدمة** — هو من يرى موقع الطلب النصي ويقرر يدويًا مدى ملاءمته له (لا فلترة جغرافية آلية في هذه المرحلة، لأن الإطلاق التجريبي في مدينة واحدة فقط).
- **صورة شخصية اختيارية** لكل المستخدمين (`profile_picture_path`، nullable)، تظهر صورة افتراضية عند عدم الرفع، قابلة للتغيير في أي وقت من الإعدادات.
- تسجيل الدخول: بريد إلكتروني + كلمة مرور فقط لكل الأدوار (لا OTP، لا هاتف).

## 4. دورة حياة الطلب (State Machine) — النسخة النهائية المعتمدة

### 4.1 الحقول التي يُدخلها كبير السن عند إنشاء الطلب
نوع الخدمة (مرافقة / تسوق-لوجستية / مرافقة طبية / مساعدة منزلية خفيفة / دعم تقني)، التصنيف (تطوع أو مدفوع + سعر مقترح رقمي إن كان مدفوعًا)، التوقيت (فوري أو مجدول بتاريخ ووقت)، تفضيل الجنس (ذكر / أنثى / لا يهم)، وصف نصي اختياري، موقع التنفيذ (عنوان نصي حر)، ومرفقات اختيارية (صور/ملفات).

### 4.2 آلية الربط (Broadcast + أول قبول)
الطلب يُعرض (Broadcast) لكل مقدمي الخدمة المؤهلين حسب **Tier فقط** وتطابق تفضيل الجنس (بدون فلترة جغرافية آلية). مقدم الخدمة يتصفح **قائمة** الطلبات المتاحة كبطاقات (وليس إشعارًا فوريًا يحتاج قرارًا سريعًا) ويختار الموافقة. **أول من يوافق يفوز**، ويُقفل الطلب فورًا أمام الباقين (يجب معالجة Race Condition على مستوى قاعدة البيانات). الطلب يبقى معلقًا **بلا حد زمني** إن لم يوافق أحد.

### 4.3 حالات الطلب (Status) — القائمة الكاملة النهائية
| الحالة | المعنى |
|---|---|
| `pending_acceptance` | بانتظار موافقة أي مقدم خدمة |
| `accepted` | وافق مقدم خدمة (لحظة القبول قبل التوكيل الرسمي) |
| `assigned` | تم التوكيل الرسمي (ربط مقدم واحد نهائيًا وإغلاق الفرصة عن الباقين) |
| `in_progress` | بدأ مقدم الخدمة التنفيذ الفعلي |
| `pending_confirmation` | أنهى مقدم الخدمة، بانتظار تأكيد كبير السن |
| `completed` | أكّد كبير السن الإكمال — نهائية |
| `provider_apologized` | اعتذر مقدم الخدمة بعد التوكيل وقبل الإنهاء |
| `provider_delayed` | حلّ موعد الخدمة ولم يبدأ مقدم الخدمة التنفيذ |
| `no_provider_found` | لم يوافق أي مقدم خدمة حتى حلول الموعد |
| `under_review` | كبير السن أبلغ عن مشكلة بعد إنهاء مقدم الخدمة، قيد المراجعة الإدارية |
| `cancelled` | ملغى نهائيًا (بأي سبب) — نهائية، مع حقل `cancellation_reason` |

### 4.4 سياسة الإلغاء — **معتمدة نهائيًا، تُلغي أي قرار سابق مخالف**
> **لا يظهر خيار الإلغاء لكبير السن إطلاقًا في الحالات الطبيعية التالية: `assigned`, `in_progress`, `pending_confirmation`** (أي ما دام مقدم الخدمة مرتبطًا ويعمل بصورة طبيعية).
> **يظهر خيار الإلغاء فقط في 3 حالات استثنائية**: `no_provider_found`، `provider_apologized`، `provider_delayed`.
> قبل التوكيل الرسمي (أي في `pending_acceptance` أو `accepted` قبل `assigned`)، يستطيع كبير السن تعديل الطلب أو إلغاءه بحرية لأنه غير مرتبط بأحد بعد.

### 4.5 مسار الاعتذار
مقدم الخدمة يعتذر (متاح له في أي وقت من `assigned` حتى قبل ضغطه "إنهاء الخدمة") ← يُفصل فورًا عن الطلب ← `status = provider_apologized` ← إشعار كبير السن ← كبير السن يختار: (أ) تعديل الطلب/الموعد وإعادة النشر (يعود لـ `pending_acceptance` من جديد بنفس الطلب، بدون طلب جديد)، أو (ب) إلغاء.

### 4.6 مسار التأخر
يحلّ موعد الخدمة المحدد ولم يبدأ مقدم الخدمة التنفيذ ← `status = provider_delayed` تلقائيًا ← إشعار كبير السن ← يختار: (أ) الانتظار لموعد جديد يحدده هو بنفسه (بلا حد أقصى)، (ب) البحث عن بديل (فصل المقدم المتأخر فورًا وإعادة النشر العاجل لنفس الطلب)، (ج) الإلغاء.

### 4.7 مسار عدم توفر مقدم من الأساس
الطلب منشور، لم يقبله أحد، ويحلّ موعد التنفيذ المحدد ← `status = no_provider_found` ← إشعار كبير السن ← يختار: تعديل/موعد جديد وإعادة نشر، أو إلغاء.

### 4.8 التأكيد والاعتراض (بعد أن ينهي مقدم الخدمة)
إنهاء مقدم الخدمة **لا يُغلق الطلب وحده**. كبير السن يجب أن: (أ) يؤكد الإكمال → `completed` + يفتح نموذج التقييم الإجباري، أو (ب) يُبلغ عن مشكلة → `under_review` (مراجعة إدارية)، أو (ج) لا يردّ → تذكيرات من النظام ثم تحويل تلقائي لمراجعة إدارية بعد مهلة معقولة (الرقم الدقيق غير محسوم بعد، اتركه قابلاً للتعديل لاحقًا في الإعدادات).

## 5. نظام التقييم — بسيط فقط (لا معايير متعددة)

- تقييم كبير السن لمقدم الخدمة: **إجباري لإغلاق الطلب كـ `completed`**. حقل واحد فقط: نجوم من 1 إلى 5 + تعليق نصي اختياري. **لا** معايير فرعية منفصلة (لا "التزام بالموعد" و"حسن تعامل" و"جودة خدمة" كحقول منفصلة — تم إلغاء هذا الاقتراح صراحة).
- تقييم مقدم الخدمة لكبير السن: اختياري، نفس الشكل البسيط، يظهر فقط في لوحة تحكم الإدارة، بلا أي تأثير آلي على النظام.

## 6. نظام الثقة — Tier فقط، بدون أي نظام نقاط منفصل

- **لا يوجد "مؤشر التزام" منفصل، ولا نظام نقاط بخصومات متفاوتة. تم إلغاء هذا الاتجاه نهائيًا بعد نقاش مطوّل.**
- مقدم الخدمة يملك مستوى ثقة واحد فقط: `tier` (1 إلى 3)، يبدأ من 1 عند الاعتماد.
- **الترقية والتخفيض يعتمدان حصريًا على معادلة واحدة: (`completed_tasks_count` + `average_rating`)** — بلا أي عامل آخر.
- **طبقة ثانية منفصلة تمامًا لا تؤثر على Tier إطلاقًا**: عدّاد بسيط `reliability_incidents_count` على `service_provider_profiles`، يزيد +1 تلقائيًا عند كل اعتذار (`provider_apologized`) أو تأخر (`provider_delayed`) أو عدم حضور نهائي مرتبط بهذا المقدم.
- عند وصول `reliability_incidents_count` إلى **3 حوادث خلال نافذة متحركة من 30 يومًا**، يرسل النظام **إشعارًا تلقائيًا للإدارة فقط** لمراجعة الحساب يدويًا. **لا يوجد أي تعليق أو إجراء تلقائي على الحساب نفسه** — القرار بيد الإدارة حصرًا.
- حقل `incident_type` (enum: `apology`, `delay`, `no_show`, nullable) يُسجَّل على مستوى الطلب نفسه (`requests`) لتوثيق أي حادثة.

## 7. الشكاوى

كل من كبير السن ومقدم الخدمة يملك حق تقديم شكوى مرتبطة بطلب معيّن. تصل جميع الشكاوى للوحة تحكم الإدارة للمراجعة واتخاذ إجراء (تحذير/تصعيد/رفض البلاغ).

## 8. شهادة التطوع

مقدم الخدمة يستطيع طلب إصدار شهادة تطوع رقمية بناءً على سجل مهامه التطوعية الموثقة، تتضمن رقمًا تسلسليًا فريدًا للتحقق لاحقًا.

## 9. الدفع

نقدًا مباشرة بين الطرفين عند اللقاء. **لا** تكامل مع بوابة دفع أو محفظة إلكترونية في هذه المرحلة (مؤجل).

## 10. الصلاحيات

| الدور | أهم الصلاحيات |
|---|---|
| كبير السن | إنشاء/تعديل/إلغاء طلبه فقط (ضمن قواعد §4.4)، تقييم إجباري، تقديم شكوى، تعديل ملفه |
| مقدم الخدمة | تصفح الطلبات المتاحة حسب Tier، قبول/اعتذار/تحديث حالة التنفيذ، تقييم اختياري، شكوى، طلب شهادة تطوع، ضبط التوفر |
| Admin | مراجعة اعتماد الحسابات، إدارة الشكاوى، تعليق/حظر حسابات، عرض كل الطلبات |
| Super Admin | كل ما سبق + إدارة حسابات الأدمن + حذف نهائي + إعدادات النظام العامة (تعديل معادلة Tier، نصوص الإشعارات) |

## 11. الجداول المعتمدة (ملخص، راجع مخطط ERD الكامل للتفاصيل الدقيقة لكل عمود)

`users`, `elder_profiles`, `service_provider_profiles` (يتضمن الآن `tier`, `completed_tasks_count`, `average_rating`, `is_available`, `reliability_incidents_count` — **بدون** `commitment_score`/`attendance_rate`/`completion_rate`/`response_rate`، وبدون جدول `provider_commitment_events` — كلها ملغاة)، `admins`, `requests` (يتضمن الآن `incident_type` وحالات الـ status الموسّعة في §4.3، وحقول توقيت: `accepted_at`, `assigned_at`, `started_at`, `completed_at`, `cancelled_at`, `cancellation_reason`)، `request_attachments`, `ratings` (بسيطة: `stars` + `comment` فقط)، `complaints`, `volunteer_certificates`, `notifications`.

## 12. التقنية

Laravel (أحدث إصدار مستقر) + Blade + Tailwind CSS + JS عادي (بدون React/Vue). عربي فقط RTL. تطبيق ويب فقط، بلا تطبيقات جوال أصلية. ~6000 مستخدم متوقع أول سنة.

## 13. ميزات مؤجلة (خارج النطاق الحالي كليًا)

التسجيل الصوتي بالذكاء الاصطناعي، بوت واتساب، حسابات مرتبطة (كبير السن + أبناؤه)، الدفع الإلكتروني، دعم لغة إضافية، تطبيقات جوال أصلية، الفلترة الجغرافية الآلية (مؤجلة لحين التوسع لأكثر من مدينة).

---

## 14. قائمة الشاشات المطلوبة (Required Screens Inventory)

**هذه القائمة هي المرجع الوحيد للحكم على أي صفحة في مجلد `frontend/`: هل هي مطلوبة، أم زائدة، أم مفقودة.**

| # | الشاشة | الفئة | ملاحظات وظيفية مختصرة |
|---|---|---|---|
| 1 | تسجيل الدخول | عام | بريد + كلمة مرور، توجيه حسب `role` بعد الدخول |
| 2 | إنشاء حساب كبير السن | كبير السن | + رفع إثبات هوية |
| 3 | إنشاء حساب مقدم الخدمة | مقدم الخدمة | + هوية + شهادة حسن سيرة وسلوك + تاريخ ميلاد |
| 4 | "حسابك قيد المراجعة" | مشتركة | تظهر لأي حساب بحالة `pending` |
| 5 | الرئيسية لكبير السن | كبير السن | زر بارز "طلب مساعدة" + ملخص طلباته |
| 6 | إنشاء طلب جديد | كبير السن | نموذج متعدد الخطوات، راجع الحقول في §4.1 |
| 7 | "طلباتي" لكبير السن | كبير السن | تبويبات: نشطة / بحاجة لإجراء / مكتملة / ملغاة |
| 8 | تفاصيل طلب واحد (كبير السن) | كبير السن | أزرار تتغيّر حسب `status` (راجع §4.4 لقاعدة الإلغاء) |
| 9 | نموذج التقييم | كبير السن | نجوم 1-5 + تعليق اختياري فقط — لا معايير متعددة |
| 10 | تقديم شكوى | مشتركة | نموذج بسيط مرتبط بطلب |
| 11 | الرئيسية لمقدم الخدمة | مقدم الخدمة | ملخص: التقييم، عدد المهام، طلبات متاحة |
| 12 | "الطلبات المتاحة" | مقدم الخدمة | بطاقات، فلترة حسب نوع الخدمة، قبول/تجاوز |
| 13 | "طلباتي" لمقدم الخدمة + التفاصيل | مقدم الخدمة | تحديث الحالة (بدء التنفيذ/إنهاء/اعتذار) |
| 14 | الأداء والتقييم | مقدم الخدمة | متوسط النجوم، عدد المهام، Tier الحالي فقط (**بدون** أي رسم/نسبة "مؤشر التزام") |
| 15 | شهادة التطوع | مقدم الخدمة | طلب إصدار + عرض الشهادات السابقة |
| 16 | إعدادات التوفر | مقدم الخدمة | أيام/ساعات العمل |
| 17 | الملف الشخصي والإعدادات | مشتركة | صورة شخصية، بيانات أساسية، كلمة مرور |
| 18 | الإشعارات | مشتركة | قائمة موحّدة |
| 19 | لوحة تحكم الإدارة | Admin | إحصائيات عامة |
| 20 | مراجعة طلبات اعتماد الحسابات | Admin | لكبار السن ومقدمي الخدمة معًا |
| 21 | إدارة كل الطلبات | Admin | عرض شامل للمتابعة |
| 22 | إدارة الشكاوى | Admin | + إشعارات `reliability_incidents_count` (§6) تظهر هنا |
| 23 | إدارة حسابات المستخدمين | Admin | تعليق/حظر |
| 24 | إدارة حسابات الأدمن | Super Admin فقط | إضافة/حذف Admin |
| 25 | إعدادات النظام العامة | Super Admin فقط | تعديل معادلة Tier، نصوص الإشعارات |

---

*نهاية المرجع الشامل. أي معلومة غير موجودة هنا وواردة في ملفات/برومبتات سابقة تُعتبر إما مؤجلة (§13) أو ملغاة صراحة إن وردت هنا كـ"ملغاة".*
