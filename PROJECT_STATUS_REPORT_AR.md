# تقرير حالة مشروع «إحسان»

**تاريخ المراجعة:** 17 أغسطس 2026  
**النطاق:** نسخة العمل المحلية على الفرع `main` عند الالتزام `bbe89e4`  
**منهجية التقرير:** فحص بنية المستودع، والتوثيق، والكود، والمخطط، والاختبارات، وإعدادات البناء، وحالة Git وسجله، ثم تشغيل فحوص قراءة/تحقق آمنة. لم تُعدّل ملفات المشروع باستثناء إنشاء هذا التقرير.

## 1. الملخص التنفيذي

**حقيقة مؤكدة:** المشروع تطبيق ويب عربي باسم «إحسان» مبني على Laravel، ويعرّف نفسه في الواجهة كمنصة تربط كبار السن بالمتطوعين الموثوقين لتقديم المساندة اليومية والخدمات الإنسانية. الدليل المباشر موجود في `resources/views/welcome.blade.php`، بينما `README.md` ما زال نص Laravel الافتراضي ولا يصف المنتج.

**الحالة الحالية المؤكدة:** توجد نواة MVP عاملة تغطي الصفحة التعريفية، واختيار نوع الحساب، وتسجيل كبير السن أو المتطوع، ورفع وثائق المتطوع، وتسجيل الدخول والخروج، واستعادة كلمة المرور، ولوحة تحكم أولية، وتحديث الملف الشخصي وحذف الحساب. طبقة البيانات تدعم نوعي الحساب وملف تسجيل واحد لكل مستخدم. اجتازت الحزمة الحالية 29 اختبارًا (76 assertion).

**استنتاج:** المشروع في مرحلة **MVP مبكرة إلى متوسطة**. رحلة إنشاء الحساب وإدارته مكتملة بدرجة جيدة، لكن القيمة التشغيلية الأساسية الموعودة للمنصة—طلب خدمة، إسناد/مطابقة متطوع، متابعة الطلب، مراجعة الوثائق أو إدارة المنصة—غير موجودة في الكود الذي تمت مراجعته. لوحة التحكم تعرض بيانات وحالة أولية أكثر من كونها مركزًا لإدارة خدمات فعلية.

## 2. التقنيات والأدوات المستخدمة

| المجال | التقنية/الإصدار المثبت أو المعرّف | الدليل |
|---|---|---|
| الخلفية | PHP `^8.2`؛ الإصدار المحلي 8.2.12 | `composer.json` ونتيجة `php --version` |
| إطار العمل | Laravel `^12.0`؛ المثبت 12.64.0 | `composer.json` و`composer.lock` و`php artisan --version` |
| قاعدة البيانات | SQLite افتراضيًا؛ SQLite داخل الذاكرة للاختبارات | `.env.example` و`phpunit.xml` و`database/migrations/` |
| ORM والمصادقة | Eloquent، Laravel Auth، Laravel Breeze 2.4 للتطوير | `app/Models/` و`app/Http/Controllers/Auth/` و`composer.json` |
| الواجهة | Blade، Tailwind CSS 3.1، Alpine.js 3.4، Axios | `resources/views/` و`tailwind.config.js` و`resources/js/app.js` و`package.json` |
| البناء | Vite 7 مع `laravel-vite-plugin` 2 | `vite.config.js` و`package.json` |
| الاختبارات | Pest 3 وPHPUnit؛ Laravel Pest Plugin | `tests/` و`phpunit.xml` و`composer.json` |
| جودة القوالب | Prettier وBlade Formatter | `.bladeformatterrc.json` و`package.json` |
| أدوات التطوير | Composer 2.9.7، Node 24.12.0، npm 11.6.2 محليًا | نتائج أوامر الإصدارات |

لا توجد ملفات CI/CD أو حاويات أو وصف نشر ظاهرة مثل `.github/workflows/` أو `Dockerfile` أو ملف Compose ضمن الملفات المفحوصة.

## 3. بنية المشروع وأهم مكوّناته

- `app/Http/Controllers/Auth/`: دورة المصادقة؛ التسجيل، الدخول والخروج، إعادة ضبط كلمة المرور، تأكيدها والتحقق من البريد.
- `app/Http/Controllers/Frontend/RegistrationPageController.php`: عرض صفحة اختيار الحساب ونموذجي التسجيل، مع تحويل روابط المراحل القديمة إلى النموذج الموحد.
- `app/Http/Controllers/ProfileController.php`: عرض الملف وتحديث بيانات المستخدم وملف التسجيل وحذف الحساب ووثائقه.
- `app/Http/Requests/`: قواعد دخول المستخدم وتحديث ملفه، بما في ذلك الحد من محاولات الدخول.
- `app/Models/User.php` و`app/Models/RegistrationProfile.php`: النموذجان المجالِيان الوحيدان؛ علاقة `hasOne`/`belongsTo` تربط المستخدم بتفاصيل التسجيل.
- `app/Notifications/EhsanResetPasswordNotification.php`: رسالة استعادة كلمة مرور بعلامة «إحسان» وقالب عربي.
- `database/migrations/`: جداول Laravel الأساسية، وحقل `account_type`، وجدول `registration_profiles`.
- `resources/views/`: الواجهة العربية RTL، وتشمل الهبوط والتسجيل والدخول ولوحة التحكم والملف وقوالب المصادقة والبريد.
- `routes/web.php` و`routes/auth.php`: 30 مسارًا مسجلًا وقت الفحص، تشمل الواجهة والمصادقة والملف ومسارات Laravel الخدمية.
- `tests/Feature/` و`tests/Unit/`: اختبارات المصادقة والتسجيل والملف واختبارا مثال افتراضيان.
- `public/assets/img/`: خمس صور ثابتة مستخدمة في الواجهة.
- `config/` و`bootstrap/`: إعداد Laravel القياسي. `package.json` و`vite.config.js` و`tailwind.config.js` تضبط سلسلة بناء الواجهة.

## 4. الميزات والأجزاء المنجزة

### حقائق مؤكدة

1. **صفحة هبوط عربية ومتجاوبة:** تعرض رسالة المنتج، أسباب الاستخدام، خطوات البدء وروابط الدخول والتسجيل. الأدلة: `resources/views/welcome.blade.php` و`public/assets/img/hero-image.jpeg`.
2. **اختيار نوع الحساب:** صفحة تفصل بين «كبير سن» و«متطوع». الأدلة: `routes/web.php` و`resources/views/frontend/register.blade.php`.
3. **تسجيل كبير السن:** يجمع الاسم والبريد وكلمة المرور وتاريخ الميلاد والهاتف والمدينة والعنوان ونوع السكن ومعلومات إضافية، مع تحقق خلفي وتخزين داخل معاملة قاعدة بيانات. الأدلة: `resources/views/frontend/elderly-register.blade.php` و`app/Http/Controllers/Auth/RegisteredUserController.php`.
4. **تسجيل المتطوع ووثائقه:** يجمع رقم الهوية ويرفع وثيقة هوية ووثيقة حسن سيرة بصيغ JPG/JPEG/PNG/PDF وبحد أقصى 5 MB للملف، ويحذف الملفات المرفوعة إذا فشلت معاملة إنشاء السجل. الأدلة: `resources/views/frontend/volunteer-register.blade.php` و`app/Http/Controllers/Auth/RegisteredUserController.php`.
5. **نموذج بيانات مخصص:** حقل `account_type` بقيمتي `elderly` و`volunteer`، وجدول ملف تسجيل بعلاقة واحد لواحد وقيد فريد لرقم الهوية وحذف متسلسل عند حذف المستخدم. الأدلة: `database/migrations/2026_08_17_000000_add_account_type_to_users_table.php` و`database/migrations/2026_08_17_000001_create_registration_profiles_table.php`.
6. **مصادقة كاملة أساسية:** دخول وخروج، تحديد معدل محاولات الدخول، نسيان/إعادة ضبط كلمة المرور، تأكيد كلمة المرور ومسارات تحقق البريد. الأدلة: `routes/auth.php` و`app/Http/Requests/Auth/LoginRequest.php` و`app/Http/Controllers/Auth/`.
7. **تعريب المصادقة واستعادة كلمة المرور:** رسائل عربية وقالب بريد مخصص. الأدلة: `lang/ar/auth.php` و`lang/ar/passwords.php` و`app/Notifications/EhsanResetPasswordNotification.php` و`resources/views/emails/auth/reset-password.blade.php`.
8. **لوحة تحكم أولية حسب نوع الحساب:** ترحيب وحالة الحساب ونوعه وبعض بيانات الاتصال، مع رابط تعديل الملف. الدليل: `resources/views/dashboard.blade.php`.
9. **إدارة الملف الشخصي:** تحديث بيانات الحساب والملف، تغيير كلمة المرور، وحذف الحساب بعد التحقق من كلمة المرور مع محاولة حذف الوثائق المرتبطة. الأدلة: `app/Http/Controllers/ProfileController.php` و`app/Http/Requests/ProfileUpdateRequest.php` و`resources/views/profile/`.
10. **واجهات RTL وهوية بصرية موحدة:** معظم القوالب عربية باتجاه RTL وألوان وهوية «إحسان»، مع تنسيق Blade آلي. الأدلة: `resources/views/layouts/` و`resources/views/frontend/` و`.bladeformatterrc.json`.

## 5. التغييرات الحديثة من سجل Git

حالة Git قبل إنشاء التقرير كانت نظيفة: `main` متطابق مع `origin/main`، بلا ملفات معدلة أو غير متتبعة. بعد إنشاء التقرير، التغيير المتوقع الوحيد هو هذا الملف الجديد.

| التاريخ | الالتزام | الخلاصة المستندة إلى السجل والتغيير |
|---|---|---|
| 2026-08-17 | `bbe89e4` | إصلاح تنسيق قوالب Blade والتنقل الأمامي؛ 38 ملفًا، +2299/-420. أضيفت أدوات فحص Blade وعُدّلت غالبية الواجهات والتخطيطات. |
| 2026-08-17 | `e7d9166` | بناء تدفقات تسجيل «إحسان» والهبوط ولوحة التحكم والملف؛ 11 ملفًا، +112/-618. وحّد التسجيل وحذف صفحات المراحل المنفصلة القديمة. |
| 2026-08-17 | `7100670` | إضافة/ترتيب المصادقة والدخول؛ تغيير واسع في 154 ملفًا، +425/-3192، وتضمن إزالة ملفات Debugbar متتبعة سابقًا وإضافة نموذج ملف التسجيل وترحيلاته. |
| 2026-08-10 | `17154af` | تحديث صفحات الدخول؛ 74 ملفًا، +8091/-24. |
| 2026-08-03 | `73d924d` | التزام مبكر بعنوان `firt commit`، محدود جدًا وفق الإحصاء. |

الفروع المحلية الموجودة: `main` عند `bbe89e4`، و`first-mvp` عند `2245951`، و`register-page` عند `45fa74b`، و`login-feature` عند `8afa38c`. المرجع البعيد الظاهر الوحيد هو `origin/main`. لا يثبت وجود الفروع المحلية وحده أنها ما زالت قيد العمل؛ يلزم قرار صيانة/دمج من الفريق.

## 6. الأعمال الجارية أو غير المكتملة

### حقائق مؤكدة

- لم يعثر البحث النصي على علامات `TODO` أو `FIXME` أو `HACK` أو `WIP` في كود المشروع بعد استثناء الاعتماديات وملفات القفل. غياب العلامات لا يعني اكتمال النطاق.
- مسارات المراحل القديمة `frontend/elderly/housing` و`review` و`frontend/volunteer/documents` و`review` لا تملك صفحات مستقلة الآن؛ تحوّل إلى نموذج التسجيل الموحد عبر `RegistrationPageController.php`.
- لا توجد نماذج أو جداول أو متحكمات لطلبات المساعدة، أو الخدمات، أو المطابقة، أو قبول المهمة، أو حالة التنفيذ، أو التقييمات، أو الإشعارات التشغيلية، أو لوحة إدارة. النماذج المجالِية الموجودة فقط هي `User` و`RegistrationProfile`.
- `resources/views/dashboard.blade.php` يعرض معلومات حساب ثابتة/ملخصة ولا ينفذ سير خدمات بين الطرفين.
- `README.md` افتراضي ولا يقدم تعليمات إعداد خاصة بالمشروع، أو وصف النطاق، أو قرارات التصميم، أو خطوات النشر.
- لا توجد إعدادات CI/CD أو نشر موثقة في المستودع.

### استنتاج

العمل الحالي ركّز على **تهيئة المستخدم والهوية البصرية**. الجزء الذي يحول المنصة من إدارة حسابات إلى خدمة ربط فعلية بين كبير السن والمتطوع لم يبدأ بصورة قابلة للرصد في الفرع الحالي.

## 7. نتائج الاختبارات والبناء

نُفذت الأوامر التالية في 17 أغسطس 2026:

| الأمر | النتيجة |
|---|---|
| `php artisan test` | **نجح:** 29 اختبارًا، 76 assertion، المدة 3.98 ثانية. تشمل التسجيل للفئتين ورفع الوثائق والمصادقة واستعادة كلمة المرور والملف والحذف. |
| `npm.cmd run check:blade` | **نجح:** جميع ملفات Blade المطابقة منسقة. |
| `composer validate --no-check-publish` | **نجح:** `composer.json` صالح. |
| `php artisan route:list --except-vendor` | **نجح:** 30 مسارًا مسجلًا. |

الاختبارات تستخدم SQLite داخل الذاكرة، وذاكرة مؤقتة وجلسات مصفوفية، وبريدًا مصفوفيًا وفق `phpunit.xml`؛ لذلك لم تعتمد على قاعدة بيانات التشغيل المحلية.

**لم يُشغّل `npm run build`:** لأن Vite يكتب المخرجات إلى `public/build`، وشرط المراجعة يمنع تغيير ملفات المشروع. وجود هذا المسار في `.gitignore` لا يجعل تشغيل البناء بلا أثر محلي. لذلك لا توجد في هذه المراجعة نتيجة مؤكدة لبناء أصول الإنتاج.

**لم تُختبر:** بيئة إنتاج فعلية، خادم بريد حقيقي، تخزين سحابي، متصفح/اختبارات end-to-end، أداء، وصولية، أو نشر. لا يمكن تأكيد جاهزيتها من الأدلة الحالية.

## 8. المشاكل التقنية والمخاطر والديون التقنية

### مخاطر مرتفعة

1. **التحقق من البريد غير مفعّل فعليًا على النموذج:** `User.php` يترك تطبيق `MustVerifyEmail` معلقًا، بينما `routes/web.php` يضع `verified` على لوحة التحكم وتوجد مسارات واختبارات تحقق البريد. في Laravel، هذا يعني أن وسيط التحقق لا يفرضه على نموذج لا يطبق العقد. النتيجة المحتملة: مستخدم جديد يصل إلى لوحة التحكم دون تحقق، رغم أن البنية توحي بعكس ذلك.
2. **لا توجد آلية مراجعة وثائق المتطوع:** النظام يخزن الوثائق، لكن لا توجد حالة مراجعة أو صلاحيات مدير أو قبول/رفض. وصف الواجهة «متطوعون موثوقون» في `welcome.blade.php` لا تدعمه بعد دورة تحقق تشغيلية في الكود.
3. **حساسية الوثائق الشخصية:** ملفات الهوية وحسن السيرة تُخزن على قرص `public` حسب `RegisteredUserController.php`. حتى لو كانت الأسماء عشوائية، تصميم التخزين العام مخاطرة خصوصية وأمن وصول لبيانات شديدة الحساسية؛ يلزم تخزين خاص وتنزيل مصرح ومراقب.

### مخاطر متوسطة

4. **عدم اتساق حذف الحساب والملفات:** `ProfileController.php` يحذف سجل المستخدم قبل حذف الملفات. إذا فشل حذف الملفات، يبقى يتيم تخزين بلا مرجع؛ وإذا فشل حذف قاعدة البيانات بعد خطوات لاحقة لا توجد معاملة توحد العمليتين. توجد معالجة أفضل لفشل التسجيل، لكنها لا تغطي الحذف.
5. **حالة تحقق البريد مربكة:** تحديث البريد يضع `email_verified_at` إلى `null` في `ProfileController.php`، لكن عدم تطبيق `MustVerifyEmail` يجعل إعادة التحقق غير مفروضة عمليًا.
6. **قواعد تحقق الهاتف والهوية محدودة:** يتم التحقق منهما كسلاسل بطول أقصى فقط، دون صيغة أو تطبيع. الأدلة: `RegisteredUserController.php` و`ProfileUpdateRequest.php`.
7. **اختبارات التسجيل لا تتحقق بالكامل من الحساب المرتبط:** اختبار تخزين بيانات كبير السن يفحص حقولًا غير فريدة دون تقييد `user_id`، ما قد يخفي خطأ ربط في سيناريو أكثر تعقيدًا. الدليل: `tests/Feature/Auth/RegistrationTest.php`.
8. **اختبارات ناقصة للحالات السلبية الحرجة:** لا تظهر اختبارات لحجم/نوع ملف غير صالح، أو فشل المعاملة وتنظيف الملفات، أو تفرد الهوية، أو قواعد الحقول الشرطية، أو حذف وثائق المتطوع عند حذف الحساب.
9. **إعدادات افتراضية حساسة للتشغيل:** `.env.example` يستخدم جلسات وطوابير وذاكرة مؤقتة في قاعدة البيانات، لكن لا توجد وثائق تشغيل عامل queue أو تهيئة بيئة إنتاج. البريد مضبوط على `log` افتراضيًا، فلا يثبت إرسال رسائل حقيقية.

### ديون منخفضة/صيانة

10. `README.md` لا يوثق المشروع، والمتطلبات، والإعداد، والحسابات، والتخزين، والاختبارات أو النشر.
11. ما زال `composer.json` يحمل اسم ووصف قالب Laravel (`laravel/laravel` و`The skeleton application...`) بدل بيانات المنتج.
12. يوجد `DatabaseSeeder.php` ينشئ مستخدمًا تجريبيًا ببريد معروف وكلمة مرور المصنع الافتراضية عند تشغيل seed؛ يجب حصره في التطوير وعدم استخدامه عرضًا في الإنتاج.
13. توجد فروع محلية قديمة غير موثقة الغرض أو الحالة، ما يزيد غموض خط التطوير، لكن لا يجوز اعتبارها مهجورة دون قرار الفريق.
14. اختبارا `tests/Unit/ExampleTest.php` و`tests/Feature/ExampleTest.php` ما زالا اختبارات قالب عامة وقيمتهما التغطوية محدودة.

## 9. الخطوات التالية المقترحة حسب الأولوية

1. **P0 — حماية الهوية والوثائق:** نقل وثائق المتطوع إلى تخزين خاص، وإضافة تنزيل مصرح، وسياسة وصول، وسجل تدقيق، وحالة مراجعة واضحة، واختبارات أمنية مرتبطة بها.
2. **P0 — حسم سياسة تحقق البريد:** تطبيق `MustVerifyEmail` واختبار منع الوصول حتى التحقق، أو إزالة وسيط ومسارات التحقق إن لم يكن مطلوبًا؛ لا تترك السلوك الحالي الملتبس.
3. **P0 — تحديد وبناء جوهر MVP:** توثيق حالات استخدام طلب المساعدة ثم إضافة كيانات الخدمة/الطلب/الإسناد والحالات والصلاحيات والتدفقات بين كبير السن والمتطوع.
4. **P1 — إضافة إدارة ومراجعة المتطوعين:** أدوار وصلاحيات، قائمة مراجعة، قبول/رفض، وحجب الوصول للمهام قبل الاعتماد.
5. **P1 — توسيع الاختبارات:** الحالات السلبية للرفع والتحقق الشرطي والتفرد وتنظيف الملفات والحذف، ثم اختبارات end-to-end للرحلتين واختبارات تفويض.
6. **P1 — تثبيت الحذف المتناسق:** تصميم معالجة فشل تضمن عدم ترك وثائق أو سجلات يتيمة، مع اختبارات صريحة.
7. **P1 — توثيق المشروع:** استبدال README الافتراضي بهدف المنتج، المتطلبات، إعداد SQLite/التخزين والبريد والطابور، أوامر الاختبار والبناء، ونطاق MVP.
8. **P2 — إعداد جودة وتسليم مستمر:** CI يشغّل الاختبارات و`composer validate` وفحص Blade وبناء Vite، ثم توثيق بيئة النشر والمتغيرات والأذونات والنسخ الاحتياطي.
9. **P2 — تحسين جودة البيانات:** قواعد وصيغ للهاتف والهوية، تطبيع المدخلات، ورسائل تحقق عربية واضحة واختبارات لها.
10. **P2 — تنظيف الصيانة:** تحديث بيانات `composer.json`، استبدال اختبارات المثال، ومراجعة الفروع المحلية وتوثيق قرار الاحتفاظ أو الدمج دون حذف تلقائي.

## 10. تقدير نسبة الإنجاز

**التقدير الاستنتاجي: نحو 58% من MVP وظيفي أولي، بهامش عدم يقين ±10 نقاط.** هذا ليس رقمًا مستخرجًا من خطة رسمية؛ لا توجد خارطة طريق أو قائمة متطلبات في المستودع. لذلك بُني التقدير فقط على وعد المنتج الظاهر في `welcome.blade.php` وعلى ما يمكن إثباته في الكود:

| المحور المفترض للـMVP | الوزن | المنجز المقدر | الأساس |
|---|---:|---:|---|
| الأساس التقني والمصادقة | 20% | 18% | Laravel وإعدادات ومصادقة واختبارات ناجحة؛ مع فجوة تحقق البريد |
| التسجيل وملفات الفئتين | 25% | 23% | تدفقان وتخزين ووثائق واختبارات؛ دون مراجعة وثائق |
| الملف ولوحة الحساب | 15% | 12% | تحديث/حذف ولوحة أولية؛ لا أنشطة تشغيلية |
| طلب الخدمة والمطابقة والتنفيذ | 25% | 0% | لا توجد كيانات أو مسارات أو واجهات داعمة |
| الإدارة والثقة والصلاحيات | 10% | 0% | لا توجد دورة اعتماد أو لوحة إدارة |
| الجودة والتوثيق والنشر | 5% | 5% تقريبًا | اختبارات وفحص تنسيق ناجحان، مقابل README ونشر وCI غير مكتملة |

المجموع التقريبي وفق هذا الترجيح هو **58%**. إذا كان النطاق المقصود مجرد التسجيل وإدارة الحسابات، فالنسبة أعلى بكثير؛ أما إذا كان يشمل منصة ربط وتشغيل كاملة كما توحي صفحة الهبوط، فهذا التقدير معقول وربما متفائل حتى تُوثق المتطلبات.

## خلاصة التحقق

- المنتج الحالي قابل للإثبات كنواة تسجيل ومصادقة وملف شخصي عربية ومختبرة.
- الاختبارات الحالية كلها ناجحة، وكذلك تحقق Composer وتنسيق Blade.
- المكوّن التشغيلي الذي يربط كبار السن بالمتطوعين غير موجود في الفرع الحالي.
- أكبر الأولويات: خصوصية وثائق المتطوعين، حسم تحقق البريد، ثم بناء طلبات الخدمة والمطابقة والإدارة.
- تعذّر تأكيد بناء الإنتاج والنشر والبريد الحقيقي وE2E؛ لم تُشغّل أوامر تغير ملفات المشروع أو بياناته.
