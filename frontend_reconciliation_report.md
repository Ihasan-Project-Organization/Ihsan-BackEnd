# تقرير مطابقة واجهات الفرونت إند مع منطق النظام المعتمد
> **وثيقة المقارنة المرجعية الثلاثية:** القسم 14 من المرجع الشامل النهائي للمشروع مقابل التنفيذ الفعلي الحالي (Blade + Controllers) وتصميم فريق الفرونت إند (`Frontend-main`).
> **تاريخ التقرير:** 2026-09-06
> **القاعدة الحاكمة:** المنطق دائمًا من التنفيذ الحالي، والتصميم البصري دائمًا من فريق الفرونت إند.

---

## 1. جدول المقارنة الشامل لشاشات القسم 14

| # | الشاشة | موجودة في التنفيذ الحالي؟ | موجودة في فرونت الفريق؟ | التوصية |
|---|---|---|---|---|
| 1 | **تسجيل الدخول** (عام) | ✅ **نعم** (`frontend/login.blade.php` و `auth/login.blade.php`) — مربوط بـ `AuthenticatedSessionController` وتوجيه الأدوار عبر `EnsureAccountApproved`، ويدعم التحقق من الحساب المعتمد وعرض رسالة الرفض `$errors->has('rejection_reason')`. | ✅ **نعم** (`login.html`) — تصميم جذاب منقسم (صورة وفورم)، لكنه يحتوي حقل اسم مستخدم `uname` بدلاً من البريد الإلكتروني، ويفتقر لـ `@csrf` ومعالجة الأخطاء. | ⚠️ **دمج مطلوب**: خذ التصميم البصري المقسم والأبعاد من `login.html`، لكن يجب استبدال حقل `uname` بحقل `email` وفقاً لـ PRD §3، مع نقل `@csrf`، ورسائل الخطأ `$errors->first('email')` و `$errors->first('rejection_reason')`، وربط الرابط بـ `route('password.request')`. |
| 2 | **إنشاء حساب كبير السن** (كبير السن) | ✅ **نعم** (`frontend/elderly-register.blade.php`) — يرسل إلى `RegisteredUserController::store` بدور `role=elder`، ويشمل رفع إثبات الهوية الإلزامي `id_document`، ويضبط `status = pending`، ويرسل إشعار التحقق من البريد أولاً. | ✅ **نعم جزئياً** (مجزأة على 3 ملفات: `siginold.html`، `siginoldage.html`، `old.html`) — تصميم بصري ممتاز بخط Alexandria وهيدر أنيق، لكنه مقسم لصفحات HTML ثابتة تفتقر لرفع الهوية الإلزامي ولا ترسل البيانات لخادم الباك إند. | ⚠️ **دمج مطلوب**: اعتمد الهيدر والبطاقات ونمط الخطوط والستيبر البصري من ملفات الفرونت إند، ولكن ادمجها في صفحة Blade واحدة بنظام ستيبر تفاعلي يرسل نموذجاً واحداً مع `enctype="multipart/form-data"`، مع الحفاظ على حقل رفع الهوية الإلزامي `id_document` وبيانات السكن وحالة الحساب `pending`. |
| 3 | **إنشاء حساب مقدم الخدمة** (مقدم الخدمة) | ✅ **نعم** (`frontend/volunteer-register.blade.php`) — يرسل إلى `RegisteredUserController::store` بدور `role=volunteer`، مع تحقق برمجي إلزامي من شرط السن (18+ سنة كاملة عبر `dob`)، ورفع وثيقتي الهوية `id_document` وحسن السيرة `good_conduct_certificate`، وتهيئة `tier = 1`. | ✅ **نعم جزئياً** (موزعة على `vo.html`، `revinfo.html`، `volchek.html`) — كروت رفع ملفات ممتازة مع شارة الاختيار الخضراء (`check-icon`)، لكنها صفحات منفصلة بروابط ثابتة، وتفتقر لمعادلة فحص السن 18+ وحقول التحقق البرمجية. | ⚠️ **دمج مطلوب**: استخدم التصميم البصري الأنيق لكروت رفع الوثائق والهيدر من `vo.html` و `revinfo.html`، وادمجها في ملف Blade موحد يرسل لـ `route('register')`، مع الالتزام التام بنقل التحقق من السن `18+` في الواجهة والكنترولر، وحفظ مسارات الوثائق في `service_provider_profiles`. |
| 4 | **"حسابك قيد المراجعة"** (مشتركة) | ✅ **نعم** (`auth/pending-approval.blade.php`) — شاشة مركزية محمية بـ Middleware `EnsureAccountApproved`، تعرض اسم المستخدم وإيميله وحالته `pending` مع زر تسجيل خروج آمن `@csrf` يوجه لـ `route('logout')`. | ✅ **نعم جزئياً** (`volchek.html`) — صُممت كخطوة ثالثة في تسجيل المتطوع وليست شاشة نظام مركزية، لكن تصميمها وأيقوناتها ممتازة. | ⚠️ **دمج مطلوب**: خذ التنسيق البصري الهادئ والأيقونات من `volchek.html` وطبقها على `pending-approval.blade.php` كشاشة مستقلة للمستخدمين المعلقين، مع الحفاظ الكامل على نموذج تسجيل الخروج `@csrf` الديناميكي وعرض بيانات الحساب. |
| 5 | **الرئيسية لكبير السن** (كبير السن) | ✅ **نعم** (`dashboard.blade.php`) — تعرض إحصائيات حية: الطلبات النشطة `$user->serviceRequests()->active()->count()`، والطلبات التي بحاجة لإجراء `needsAction()->count()` مع شارة وميض، والمكتملة `completed()->count()`، وزر المودال السريع `openCreateRequestModal()`. | ✅ **نعم** (`oldman_home/OldMan.html`) — لوحة تحكم متكاملة بشريط جانبي (Sidebar) متناسق، وهيدر علوي بالإشعارات، وبانر ترحيبي، وكروت سريعة ("طلب مساعدة"، "طلباتي"). خالية من الإحصائيات الحقيقية. | ⚠️ **دمج مطلوب**: اعتمد هيكل وتصميم `OldMan.html` (الشريط الجانبي، الهيدر، وكروت الإجراءات)، واحقن في داخله بطاقات الإحصائيات الحية الثلاث (`activeCount`, `needsActionCount`, `completedCount`) المستخرجة من `dashboard.blade.php`، واربط زر "طلب مساعدة" بمسار أو مودال إنشاء الطلب. |
| 6 | **إنشاء طلب جديد** (كبير السن) | ✅ **نعم** (`service-requests/partials/create-modal.blade.php`) — يرسل إلى `ServiceRequestController::store`، ويولد تلقائياً الرقم التسلسلي الفريد `#REQ-XXXX` عبر `ServiceRequest::generatePublicId()`، ويشمل كافة الحقول الأساسية. | ⚠️ **نعم جزئياً** (`oldman_home/request-help.html` ومودال `my-requests.html`) — شبكة كروت بصرية جذابة لاختيار نوع الخدمة، لكنها تقتصر على اختيار الكارت فقط وتفتقر تماماً لباقي حقول المرجع §4.1! | 🔧 **يحتاج بناء الفرونت إند لمنطق ناقص عنده أصلًا**: تصميم الفرونت إند لا يحتوي عناصر إدخال لـ: التوقيت المجدول (`scheduled_at` / `timing_type`)، تفضيل الجنس (`gender_preference`)، نوع التسعير تطوع/مدفوع (`pricing_type`, `proposed_price`)، والعنوان والمرفقات. يجب توسيع الواجهة البصرية لتشمل هذه الحقول الإلزامية قبل الدمج. |
| 7 | **"طلباتي" لكبير السن** (كبير السن) | ✅ **نعم** (`service-requests/index.blade.php`) — تقسيم التبويبات المعتمدة مع العدادات الحقيقية `$counts` (نشطة / بحاجة لإجراء / مكتملة / ملغاة / الكل)، معالجة انتهاء المواعيد التلقائية `processScheduleExpirations()`، والبحث النصي. | ✅ **نعم** (`oldman_home/my-requests.html`) — تصميم ممتاز للتبويبات وبطاقات الطلبات بحالاتها المختلفة، لكن التبويبات تعمل بـ JS محلي ثابت بدون معاملات URL (`?tab=`) أو حلقة بيانات حقيقية. | ⚠️ **دمج مطلوب**: استخدم الهيكل والتصميم البصري للتبويبات والبطاقات من `my-requests.html`، ولكن استبدل البيانات الثابتة بحلقة `@forelse($requests as $item)`، واربط التبويبات بالمتغير `$tab` وروابط `route('service-requests.index', ['tab' => ...])`، واعرض العدادات `$counts` بجانب كل تبويب. |
| 8 | **تفاصيل طلب واحد** (كبير السن) | ✅ **نعم** (مدمجة ضمن بطاقات `service-requests/index.blade.php` ومودالات `partials/action-modals.blade.php`) — تحكم شرطي دقيق بزر الإلغاء `$item->canBeCancelledByElderly()` (ممنوع في `assigned`, `in_progress`, `pending_confirmation`)، وحجب رقم هاتف المتطوع إلا بعد التوكيل `$item->canRevealContactPhone()`. | ✅ **نعم جزئياً** (داخل كروت `my-requests.html`) — تظهر بعض التفاصيل والأزرار بشكل ثابت، لكن زر الإلغاء يظهر دائماً بدون شروط، ورقم الهاتف لا يخضع لأي حماية. | ⚠️ **دمج مطلوب**: خذ تصميم الكروت الموسع من `my-requests.html`، لكن يجب نقل المنطق البرمجي الحاسم: دالة `$item->canBeCancelledByElderly()` لحجب زر الإلغاء في الحالات المحظورة، ودالة `$item->canRevealContactPhone()` لحماية هاتف مقدم الخدمة، وأزرار المسارات المعقدة (اعتذار/تأخر مقدم الخدمة). |
| 9 | **نموذج التقييم** (كبير السن) | ✅ **نعم** (مودال التقييم `#confirmModal` في `service-requests/partials/action-modals.blade.php`) — يرسل إلى `confirmCompletion`، إجباري لإغلاق الطلب، تقييم مبسط (نجوم 1-5 + تعليق اختياري) بحسب §5، ويحدث الـ `Tier` ومعدل التقييم آلياً. | ❌ **غير موجودة كنموذج** (يوجد فقط زر ثابت `قيّم المتطوع` ونجوم عرض في `my-requests.html` دون أي نافذة منبثقة أو حقول إرسال). | 🔧 **يحتاج بناء الفرونت إند لمنطق ناقص عنده أصلًا**: لا يوجد تصميم لمودال أو صفحة التقييم عند فريق الفرونت إند. يحتاج تصميم نافذة تقييم بالنجوم (1-5) مع حقل تعليق اختياري متناسقة مع هوية الموقع، لترسل لـ `route('service-requests.confirm', $item)` (PATCH). |
| 10 | **تقديم شكوى** (مشتركة) | ✅ **نعم** (مودال `#reportProblemModal` في `service-requests/partials/action-modals.blade.php`) — يرسل إلى `reportProblem`، يحول الطلب لـ `under_review`، ويفتح سجلاً في جدول `complaints` بحالة `open`. | ❌ **غير موجودة إطلاقاً** في مجلد `Frontend-main`. | 🔧 **يحتاج بناء الفرونت إند لمنطق ناقص عنده أصلًا**: يتطلب تصميم نافذة منبثقة أو صفحة بسيطة لإدخال تفاصيل المشكلة/الشكوى على الطلب، لترسل إلى `route('service-requests.report-problem', $item)`. |
| 11 | **الرئيسية لمقدم الخدمة** (مقدم الخدمة) | ✅ **نعم** (`provider/dashboard.blade.php`) — تعرض شارة المستوى (`Tier 1/2/3`)، زر تبديل التوفر `is_available`، مهام الأسبوع، إحصائيات التقييم، وتفاصيل المهمة القادمة بحماية الهاتف `canRevealContactPhone()`. | ❌ **غير موجودة إطلاقاً** في مجلد `Frontend-main` (لم يصمم الفريق أي صفحة لداخل حساب المتطوع). | *(راجع قسم شاشات منطقية جاهزة أدناه)* |
| 12 | **"الطلبات المتاحة"** (مقدم الخدمة) | ✅ **نعم** (`provider/available.blade.php`) — تصفح كروت الطلبات المؤهلة حسب المستوى والتفضيل، زر القبول الفوري محمي بقفل تشاؤمي لمنع السباق (Race Condition)، حجب تفاصيل العنوان والهاتف قبل القبول. | ❌ **غير موجودة إطلاقاً** في مجلد `Frontend-main`. | *(راجع قسم شاشات منطقية جاهزة أدناه)* |
| 13 | **"طلباتي" لمقدم الخدمة + التفاصيل** (مقدم الخدمة) | ✅ **نعم** (`provider/tasks.blade.php` و `partials/modals.blade.php`) — ستيبر خماسي المراحل بدقة، أزرار تحديث الحالة: بدء الخدمة (`startTask`)، توقع تأخير (`reportDelay`)، إنهاء الخدمة (`completeTask`)، والاعتذار (`apologize`)، وتقييم كبير السن للإدارة (`rateElder`). | ❌ **غير موجودة إطلاقاً** في مجلد `Frontend-main`. | *(راجع قسم شاشات منطقية جاهزة أدناه)* |
| 14 | **الأداء والتقييم** (مقدم الخدمة) | ✅ **نعم** (`provider/performance.blade.php`) — تطبيق حرفي لنظام الـ Tier المعتمد فقط (§6) بدون مؤشر التزام، شريط التقدم للمستوى التالي، سجل التقييمات وتوزيع النجوم. | ❌ **غير موجودة إطلاقاً** في مجلد `Frontend-main`. | *(راجع قسم شاشات منطقية جاهزة أدناه)* |
| 15 | **شهادة التطوع** (مقدم الخدمة) | ✅ **نعم** (`provider/certificates.blade.php`) — طلب إصدار شهادة برقم تسلسلي موثق (`CERT-XXXX-XXX`)، واستعراض الشهادات وطباعتها وتصديرها PDF. | ❌ **غير موجودة إطلاقاً** في مجلد `Frontend-main`. | *(راجع قسم شاشات منطقية جاهزة أدناه)* |
| 16 | **إعدادات التوفر** (مقدم الخدمة) | ✅ **نعم** (`provider/availability.blade.php`) — ضبط التوفر اللحظي `is_available`، وشرح سياسة البث والقاموس التشغيلي للحالات. | ❌ **غير موجودة إطلاقاً** في مجلد `Frontend-main`. | *(راجع قسم شاشات منطقية جاهزة أدناه)* |
| 17 | **الملف الشخصي والإعدادات** (مشتركة) | ✅ **نعم** (`profile/edit.blade.php` ومكونات Breeze) — تحديث الاسم، الإيميل، الهاتف الإلزامي، صورة الملف الشخصي، كلمة المرور، وحذف الحساب. | ✅ **نعم جزئياً** (`oldman_home/settings.html`) — تصميم جذاب لكروت القائمة، بانر البروفايل، وزر تبديل الإشعارات ومزلق الخطوط، لكنه يفتقر لحقول الإدخال الوظيفية ونماذج الإرسال. | ⚠️ **دمج مطلوب**: استغل الشكل والتنسيقات البصرية من `settings.html` (البانر، الكروت الأيقونية) لتطوير مظهر صفحة `profile/edit.blade.php`، مع المحافظة على حقول إدخال البيانات الحقيقية وتغيير كلمة المرور ورفع الصورة الشخصية `profile_picture_path`. |
| 18 | **الإشعارات** (مشتركة) | ⚠️ **المنطق موجود والواجهة ناقصة** — الإشعارات تُنشأ وتُخزن بالكامل في جدول `notifications` عند كافة الأحداث (تأخير، اعتذار، اعتماد، شكاوى)، وتظهر كشارة تنبيهات بالهيدر. | ✅ **نعم** (`oldman_home/notifications.html`) — تصميم متكامل وممتاز لشاشة الإشعارات مع تبويبات (الكل / غير مقروءة / الطلبات) وكروت إشعارات ملونة بالأيقونات. | ⚠️ **دمج مطلوب**: استخدم تصميم `notifications.html` بالكامل كواجهة Blade أساسية، وقم بربطها بكنترولر `NotificationController` لجلب إشعارات المستخدم الحقيقية من قاعدة البيانات وتحديث حالة القراءة (`is_read = true`). |
| 19 | **لوحة تحكم الإدارة** (Admin) | ❌ غير مطبقة واجهياً بعد (الموديلات والجداول جاهزة: `admins`, `users`, `requests`, `complaints`). | ❌ غير موجودة في `Frontend-main`. | 🆕 **أنشئ من الصفر** — ابنِها عند الوصول للمرحلة 4 بنظام تصميم لوحة التحكم الموحد. |
| 20 | **مراجعة طلبات اعتماد الحسابات** (Admin) | ❌ غير مطبقة واجهياً بعد (حقول `status = pending/approved/rejected` ووثائق الهوية جاهزة بالباك إند). | ❌ غير موجودة في `Frontend-main`. | 🆕 **أنشئ من الصفر** — ستُبنى لعرض وثائق كبار السن والمتطوعين مع أزرار الاعتماد والرفض مع إدخال `rejection_reason`. |
| 21 | **إدارة كل الطلبات** (Admin) | ❌ غير مطبقة واجهياً بعد (جدول `requests` جاهز بكافة حالاته). | ❌ غير موجودة في `Frontend-main`. | 🆕 **أنشئ من الصفر** — جدول تحكم شامل بمؤشرات الحالات والتفاصيل. |
| 22 | **إدارة الشكاوى** (Admin) | ❌ غير مطبقة واجهياً بعد (جدول `complaints` جاهز، وكذلك عداد الحوادث `reliability_incidents_count`). | ❌ غير موجودة في `Frontend-main`. | 🆕 **أنشئ من الصفر** — شاشة متابعة الشكاوى ومراجعة الحسابات التي تصل لـ 3 حوادث خلال 30 يوماً (§6). |
| 23 | **إدارة حسابات المستخدمين** (Admin) | ❌ غير مطبقة واجهياً بعد. | ❌ غير موجودة في `Frontend-main`. | 🆕 **أنشئ من الصفر** — تعليق وحظر وتعديل صلاحيات. |
| 24 | **إدارة حسابات الأدمن** (Super Admin) | ❌ غير مطبقة واجهياً بعد. | ❌ غير موجودة في `Frontend-main`. | 🆕 **أنشئ من الصفر** — إدارة المشرفين. |
| 25 | **إعدادات النظام العامة** (Super Admin) | ❌ غير مطبقة واجهياً بعد. | ❌ غير موجودة في `Frontend-main`. | 🆕 **أنشئ من الصفر** — تعديل معاملات Tier ونصوص التنبيهات. |

---

## 2. شاشات منطقية جاهزة تحتاج تصميمًا من الفرونت إند لاحقًا

هذه الشاشات تم بناؤها واختبارها برمجياً بالكامل في الباك إند وتعمل بمنطق سليم وموثق باختبارات آلية ناجحة بنسبة 100%، لكن **فريق الفرونت إند لم يصمم لها صفحات في مجلد `Frontend-main` إطلاقاً** (حيث انحصر عمل الفريق في صفحات التسجيل ولوحة كبير السن فقط):

1. **الرئيسية لمقدم الخدمة (Dashboard)** — [provider/dashboard.blade.php](file:///c:/Users/pc/OneDrive/Desktop/BackEnd/Ihsan_Project/resources/views/provider/dashboard.blade.php):
   - **المنطق المنفذ:** عرض المستوى `Tier`، تبديل التوفر اللحظي `is_available`، تفاصيل المهمة الحالية والقادمة، عدادات المهام والتقييم بالنجوم، والتطبيق الإلزامي لشرط حجب رقم هاتف كبير السن إلا بعد التوكيل `canRevealContactPhone()`.
   - **الوضع الحالي:** تعمل حالياً بتصميم Blade أنيق اعتمدناه في المرحلة 3، وتنتظر إما اعتماد تصميمها الحالي أو تزويدنا بقالب موحد من فريق الفرونت إند.

2. **شاشة "الطلبات المتاحة" للمتطوع (Available Requests)** — [provider/available.blade.php](file:///c:/Users/pc/OneDrive/Desktop/BackEnd/Ihsan_Project/resources/views/provider/available.blade.php):
   - **المنطق المنفذ:** استعراض كروت الطلبات المتوافقة مع Tier، فلترة حسب نوع الخدمة، وزر القبول المحمي بـ `lockForUpdate` ضد تضارب القبول اللحظي (Race Condition)، مع إخفاء العنوان التفصيلي والهاتف قبل القبول.
   - **الوضع الحالي:** جاهزة وتعمل برمجياً، وتحتاج مراجعة شكل بطاقاتها مع هوية الفرونت إند.

3. **شاشة "مهامي ومتابعة الطلبات" للمتطوع (My Tasks)** — [provider/tasks.blade.php](file:///c:/Users/pc/OneDrive/Desktop/BackEnd/Ihsan_Project/resources/views/provider/tasks.blade.php):
   - **المنطق المنفذ:** المخطط المرحلي الخماسي (ستيبر)، أزرار العمليات المعتمدة حصراً: بدء التنفيذ (`startTask`)، توقع تأخير (`reportDelay`)، إنهاء المهمة (`completeTask`)، والاعتذار (`apologize`)، والتقييم الموجه للإدارة (`rateElder`).
   - **الوضع الحالي:** مكتملة برمجياً ومنطقياً وخالية من أي حالات ملغاة قديمة.

4. **شاشة "الأداء والمستوى" (Performance & Tier)** — [provider/performance.blade.php](file:///c:/Users/pc/OneDrive/Desktop/BackEnd/Ihsan_Project/resources/views/provider/performance.blade.php):
   - **المنطق المنفذ:** عرض المستوى الحالي حصراً (`Tier 1 / 2 / 3`)، حاسبة وشريط التقدم للترقية (10 مهام بمتوسط 4.0 للمستوى 2، و30 مهمة بمتوسط 4.3 للمستوى 3)، مع الحذف النهائي والتام لأي ذكر لمؤشر الالتزام الملغى (§6).
   - **الوضع الحالي:** جاهزة برمجياً بالكامل.

5. **شاشة "شهادات التطوع الرقمية" (Certificates)** — [provider/certificates.blade.php](file:///c:/Users/pc/OneDrive/Desktop/BackEnd/Ihsan_Project/resources/views/provider/certificates.blade.php):
   - **المنطق المنفذ:** سجل الساعات التطوعية، طلب إصدار شهادة معتمدة برقم تسلسلي فريد (`CERT-XXXX-XXX`)، واستعراض وطباعة وحفظ الشهادات بتنسيق رسمي.
   - **الوضع الحالي:** مكتملة برمجياً وموثقة باختبار Feature Test.

6. **شاشة "إعدادات التوفر والقاموس التشغيلي"** — [provider/availability.blade.php](file:///c:/Users/pc/OneDrive/Desktop/BackEnd/Ihsan_Project/resources/views/provider/availability.blade.php):
   - **المنطق المنفذ:** تبديل حالة التوفر الحقيقي `is_available` في قاعدة البيانات، وعرض توضيحي للحالات الإحدى عشرة المعتمدة.

---

## 3. ملخص وتوصيات خطة الدمج القادمة

1. **شاشات التوافق المباشر:**
   - شاشة **الإشعارات** (`notifications.html`) هي الأقرب للجاهزية، تصميمها ممتاز ويحتاج فقط إنشاء Controller و Route لربطه بجدول `notifications`.
2. **شاشات الدمج الحرج (High-Risk Logic):**
   - **تسجيل الدخول (`login.html`):** يجب استبدال اسم المستخدم بالإيميل وضمان حقن التوجيه حسب الدور.
   - **تفاصيل الطلب وقائمة "طلباتي" (`my-requests.html`):** الحذر الشديد من نقل زر الإلغاء بدون شرط `$item->canBeCancelledByElderly()`، والتأكد من حماية الهاتف بشرط `$item->canRevealContactPhone()`.
   - **تسجيل كبير السن والمتطوع (`siginold`, `vo`):** إعادة تجميع الصفحات المفككة في نموذج Blade موحد متعدد الخطوات مع الحفاظ على التحقق الإلزامي من السن 18+ ورفع الوثائق.
3. **عناصر ناقصة لدى الفرونت إند يجب تصميمها:**
   - نموذج إنشاء الطلب الكامل بحقوله المعتمدة (§4.1).
   - مودال التقييم الإجباري بالنجوم لكبير السن (1-5 نجوم + تعليق).
   - مودال تقديم الشكوى على الطلب.
4. **شاشات مقدم الخدمة (6 شاشات):**
   - تم بناؤها مسبقاً بمنطق متين ويمكن إما الإبقاء على تصميمها الحالي المتوافق أو تطبيق لغة الـ Sidebar والألوان المستخرجة من `oldman_home` عليها لتوحيد التجربة.
